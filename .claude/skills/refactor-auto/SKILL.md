---
name: refactor-auto
description: >
  リファクタリングをループエンジニアリング的な仕組みで自律実行するオーケストレーター。
  「/refactor-auto <issue番号>」で呼び出す。issue番号は必須引数。

  人間のタッチポイントは最大2回（不明点への質問・変更計画の承認）。
  探索 → 質問 → 計画立案 → 計画承認 → 実装 → レビュー修正ループ（最大3回）の順で自律的に進む。

  以下の場面で必ずこのskillを使うこと:
  - 「/refactor-auto <番号>」と呼び出されたとき
  - 「issue #NNのリファクタを自律的に進めて」「ループエンジニアリングでリファクタして」と言われたとき
model: sonnet
---

## 概要

リファクタリング専用の自律ループワークフロー。
機能追加とは異なり、「既存コードを別のコード・コンポーネントに置き換える」パターンに特化する。

探索フェーズで「何をどう変えるか」を明確にし、質問→計画→承認のサイクルで人間と合意してから実装する。
実装後はレビュー→修正を最大3回繰り返し、通らなければ人間に報告して終了する。

## 引数

- `<issue番号>`（必須）: リファクタリング対象のGitHub issueの番号
- 未指定の場合: 「issue番号を指定してください。例: /refactor-auto 42」と伝えて終了する

## エージェント構成

```
メインスキル（オーケストレーター）
├── Step0: 進捗確認・再開ポイント判定（メイン）
├── Step1: 探索 → Explore エージェント
│             ↓ 調査結果・変更パターン候補・不明点リストを受け取る
├── Step2: 質問フェーズ（メイン）← タッチポイント① ※不明点がなければスキップ
├── Step3: 計画立案 → Plan エージェント
│             ↓ 変更計画書を受け取る
├── Step4: 計画承認フェーズ（メイン）← タッチポイント②
├── Step5: 実装 → general-purpose エージェント
│             ↓ 変更ファイル一覧を受け取る
├── Step6〜8: レビュー → 修正ループ（最大3回）
│   ├── [dev-auto-impl-reviewer] レビュー
│   └── [general-purpose] 修正（問題ありの場合）
└── Step9: コミット・完了報告（メイン）
```

## 手順

### Step 0: 進捗確認と再開ポイントの判定

`gh issue view <番号> --comments` でissueコメントを取得し、以下の表に従って再開ポイントを決定する。

| issueコメントの状態 | 再開ポイント |
|---|---|
| コメントなし（初回） | Step 1 から実行 |
| `## 確認済み不明点` のコメントあり（スキップした場合を含む） | Step 3 から実行 |
| `## 承認済みリファクタ計画` のコメントあり | Step 5 から実行 |
| `## リファクタ実装完了` のコメントあり | Step 6 から実行 |

再開ポイントが Step 1 以外の場合は、ユーザーに以下を伝えてから該当 Step に進む:

```
issue #<番号> の進捗を確認しました。<X> まで完了しているため、Step <N> から再開します。
```

### Step 1: 探索（Explore エージェント）

`subagent_type: "Explore"` でエージェントを起動する。以下を調査させ、結果をメインに返させる:

- issueのタイトル・本文（`gh issue view <番号>` で取得）
- リファクタ対象と思われるファイル・ディレクトリ
- 置き換え前のパターン（現在のコード）と、置き換え後のパターン（新コンポーネント・新APIなど）の候補
- 変更対象ファイルの一覧（grep・ファイル検索で特定）
- **Material Ambiguity（実装方向が変わりうる曖昧点）の洗い出し**
  - スコープ: どのディレクトリ・ファイルまで変更するか
  - 例外: 変更しないファイル・ケースがあるか
  - 変換ルール: 複数のパターンがある場合の優先度や使い分け
  - ユーザーに見える動作が変わりうる点

Exploreエージェントへの指示例:
```
issue番号 <番号> のリファクタリングを自律実行するために、以下を調査してください。

1. `gh issue view <番号>` でissueの内容を取得する
2. 変更対象と思われるファイル・ディレクトリを特定する（grep や git ls-files を使う）
3. 置き換え前のコードパターンと置き換え後のパターン候補を具体的に示す
4. 変更が必要なファイルの一覧を作成する
5. 以下の観点で「Material Ambiguity（実装方向に影響する曖昧点）」を洗い出す:
   - 変更スコープ（どこまで変えるか）
   - 例外ケース（変えないファイル・パターンがあるか）
   - 変換ルールが複数ある場合の使い分け
   - ユーザーに見える動作が変わりうる点

以下を返してください:
- 調査サマリー（対象ファイル一覧・現在のパターン・置き換えパターン）
- Material Ambiguity の一覧（なければ「なし」）
```

### Step 2: 質問フェーズ（メイン）← タッチポイント①

Explore エージェントから Material Ambiguity の一覧を受け取る。

**不明点がない場合**: このステップをスキップし、以下の内容でissueに記録してStep 3へ進む:
```
## 確認済み不明点

不明点なし。探索フェーズで変更計画を立てる十分な情報が得られました。
```

**不明点がある場合**: 不明点を **1回にまとめて** ユーザーに質問する（複数回に分けない）。
回答を得たら、以下の形式で `gh issue comment` でissueに記録する:
```
## 確認済み不明点

### 質問
- <質問1>
- <質問2>

### 回答
- <質問1への回答>
- <質問2への回答>
```

### Step 3: 計画立案（Plan エージェント）

`subagent_type: "Plan"` でエージェントを起動する。
以下を渡し、具体的な変更計画書を作成させる:

```
以下のリファクタリングの変更計画を作成してください。

[issue番号: <番号>]
※ issueの詳細は `gh issue view <番号>` で取得すること

[探索結果サマリー]
<Step 1 の調査サマリー>

[不明点への回答]
<Step 2 で記録した回答（スキップした場合は「不明点なし」）>

変更計画には以下を含めること:
1. リファクタリングの目的・ゴール（1〜2行）
2. 変換ルール（置き換え前のパターン → 置き換え後のパターンを具体的なコード例付きで）
3. 変更対象ファイル一覧（ファイルパスと変更内容の概要）
4. 変更しないファイル・スコープ外の明示
5. 注意点（変換時に気をつけるべき例外・エッジケース）

計画書を返してください（ファイルへの保存は不要）。
```

### Step 4: 計画承認フェーズ（メイン）← タッチポイント②

Plan エージェントから変更計画書を受け取り、ユーザーに提示する:

```
以下の変更計画を確認してください。
修正したい点があれば指示してください。問題なければ「OK」と返してください。

---
<変更計画書の内容>
---
```

フィードバックを受けて修正する（複数回対応）。ユーザーが「OK」を返したら:
- 以下の形式で `gh issue comment` でissueに記録する:
  ```
  ## 承認済みリファクタ計画

  ### 目的・ゴール
  <目的>

  ### 変換ルール
  <変換ルール>

  ### 変更対象ファイル
  - `<ファイルパス>`: <変更内容の概要>
  - ...

  ### スコープ外
  <変更しないファイル・範囲>

  ### 注意点
  <例外・エッジケース>

  ### フィードバック履歴
  <何をどう変えたか（変更がなければ「修正なし」）>
  ```

### Step 5: 実装（general-purpose エージェント）

`subagent_type: "general-purpose"` でエージェントを起動する。
承認済みリファクタ計画を渡し、変更を実装させる:

```
以下の承認済みリファクタ計画に従って、コードを変更してください。

[issue番号: <番号>]
※ 承認済みリファクタ計画の詳細は `gh issue view <番号> --comments` で取得すること
  （`## 承認済みリファクタ計画` のコメントを参照）

実装方針:
- 承認済みリファクタ計画の変換ルールに厳密に従う
- スコープ外のファイルは絶対に触らない
- 機能の変更は行わない（リファクタリングのみ）
- CLAUDE.md・.claude/rules/core.md のコーディング規約に従う
- フォーマットは biome 等の設定済みフォーマッタで実行する

完了後に以下を返してください:
- 変更したファイルの一覧（ファイルパスと変更内容の概要）
- 変換できなかったファイル・箇所があれば一覧と理由
```

完了後、以下の形式で `gh issue comment` でissueに記録する:
```
## リファクタ実装完了

変更したファイル:
- `<ファイルパス>`: <変更内容の概要>
- ...

変換できなかった箇所: <なければ「なし」>
```

### Step 6〜8: レビュー → 修正ループ（最大3回）

#### 各ループの流れ

1. `subagent_type: "dev-auto-impl-reviewer"` でレビュアーエージェントを起動する。渡すプロンプト:
   ```
   issue番号: <番号>
   実装ファイル一覧: <Step 5 または前回の修正で変更したファイル一覧>
   セルフレビュー結果: なし（リファクタ実装のレビューです）
   テストファイル一覧: なし

   レビューの観点:
   - 承認済みリファクタ計画（`gh issue view <番号> --comments` の `## 承認済みリファクタ計画`）の変換ルール通りに変更されているか
   - スコープ外のファイルが変更されていないか
   - 機能の変更が混入していないか
   - ビルド・型エラーが発生していないか
   - フォーマットは整っているか
   ```

2. 結果を受けて:
   - **「承認」**: Step 9へ進む
   - **「問題あり」**: `subagent_type: "general-purpose"` で修正エージェントを起動し、問題リストを渡して修正させる

#### ループ回数の管理

- ループは最大3回まで（Step6・Step7・Step8）
- 3回目のレビューで「問題あり」が返ってきた場合: **自動修正を打ち切り**、人間に報告して終了する

3回で解消できなかった場合の報告:
```
## /refactor-auto — 自動修正できませんでした

3回のレビュー・修正試行で以下の問題が解消できませんでした:

### 未解決の問題
<問題リスト>

### 試行履歴
- 試行1: <変更したファイルと変更内容>
- 試行2: <変更したファイルと変更内容>
- 試行3: <変更したファイルと変更内容>

手動での確認・修正をお願いします。
```

`gh issue comment` でissueにも同内容を記録し、終了する。

### Step 9: コミット・完了報告（メイン）

1. 変更したファイルをコミットする（コミット規約は `.claude/rules/core.md` を参照）
2. 以下の形式でユーザーに完了を報告する:

```
## /refactor-auto 完了 (#<番号>)

### 実行したフェーズ
1. 探索（変更対象・パターンの特定）
2. 質問フェーズ（<スキップ / N件の不明点を確認>）
3. 計画立案
4. 計画承認
5. 実装
6. レビュー・修正（<N回で承認>）

### 成果物
- 変更ファイル: <一覧>
- レビュー結果: <承認 / N回の修正後承認>

PRを作成する場合は以下を実行してください:
/create-pr
```

## 注意事項

- `gh` コマンドが使えない場合はエラーをそのままユーザーに伝える
- 実装フェーズでは「機能変更なし・リファクタのみ」を実装エージェントに明示すること
- レビュアーには承認済みリファクタ計画のissueコメントを必ず参照させる
- issueに記録するコメントは日本語で書く
- コミット対象は変更したファイルのみ。意図しないファイルを混入させない（`git add .` / `git add -A` は使わない）
